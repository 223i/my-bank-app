spring:
  application:
    name: transfer-service
  cloud:
    consul:
      enabled: false
  config:
    import: ""

server:
  port: 0

management:
  endpoints:
    web:
      exposure:
        include: health, info
  endpoint:
    health:
      show-details: always
