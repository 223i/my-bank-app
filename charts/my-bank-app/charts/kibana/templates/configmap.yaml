apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "kibana.fullname" . }}
  labels:
    {{- include "kibana.labels" . | nindent 4 }}
data:
  kibana.yml: |
    server.host: {{ .Values.kibana.server.host }}
    server.port: {{ .Values.kibana.server.port }}
    elasticsearch.hosts: {{ .Values.kibana.elasticsearch.hosts | toJson }}
