apiVersion: v1
kind: Service
metadata:
  name: cash-service
spec:
  type: ClusterIP
  ports:
    - port: 8084
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: cash-service
