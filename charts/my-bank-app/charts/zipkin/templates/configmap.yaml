apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "zipkin.fullname" . }}
  labels:
    {{- include "zipkin.labels" . | nindent 4 }}
data:
  ZIPKIN_STORAGE_TYPE: {{ .Values.zipkin.storage.type | quote }}
