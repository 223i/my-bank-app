apiVersion: v1
kind: Service
metadata:
  name: common-db
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: postgres
      protocol: TCP
      name: postgres
  selector:
    app: common-db