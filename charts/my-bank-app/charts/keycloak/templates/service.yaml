apiVersion: v1
kind: Service
metadata:
  name: keycloak
spec:
  type: ClusterIP
  ports:
    - port: 8080      # Внутренний порт сервиса
      targetPort: 8080 # Порт внутри контейнера Keycloak
      protocol: TCP
      name: http
  selector:
    app: keycloak
